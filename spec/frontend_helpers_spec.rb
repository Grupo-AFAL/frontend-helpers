# frozen_string_literal: true

RSpec.describe FrontendHelpers do
  it 'has a version number' do
    expect(FrontendHelpers::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(false)
  end
end
