# frozen_string_literal: true

source 'https://rubygems.org'

# Specify your gem's dependencies in frontend_helpers.gemspec
gemspec

gem 'rake', '~> 12.0'
gem 'ransack'

group :development, :test do
  gem 'byebug'
end

group :development do
  gem 'rubocop', require: false
  gem 'sqlite3'
end

group :test do
  gem 'rspec', '~> 3.0'
end
